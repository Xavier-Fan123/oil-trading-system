# Grafana Dashboard Provisioning Configuration
apiVersion: 1

providers:
  # Oil Trading System Dashboards
  - name: 'oil-trading-dashboards'
    orgId: 1
    type: file
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards